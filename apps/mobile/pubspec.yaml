name: booking_code
description: "Flutter client for the Betway Nigeria booking code API."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.2

dependencies:
  dio: ^5.11.0
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  freezed_annotation: ^3.1.0
  get_it: ^9.2.1
  google_fonts: ^8.2.1
  intl: ^0.20.3
  json_annotation: ^4.12.0
  lucide_icons: ^0.257.0
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.15.1
  # Exact-pinned: `^3.2.5` resolves to the 3.2.6-dev.1 prerelease (pub's solver doesn't
  # exclude it under this constraint), and freezed 4.x is a very recent major this codebase
  # hasn't been checked against.
  freezed: 3.2.5
  json_serializable: ^6.14.1
  bloc_test: ^10.0.0
  mocktail: ^1.0.5

flutter:
  uses-material-design: true
